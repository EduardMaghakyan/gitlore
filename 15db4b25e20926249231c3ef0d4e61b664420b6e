[agent-assisted] The developer wanted to make gitlore easier to install by documenting the Homebrew tap alongside the existing Go install method. The README now includes a Homebrew section with the correct tap URL and install command, positioned above the Go install instructions. No work was left incomplete, though the Homebrew tap itself was assumed to already exist and was not created as part of this session.
