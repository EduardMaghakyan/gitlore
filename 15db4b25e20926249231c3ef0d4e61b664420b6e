[agent-assisted] The developer wanted to make gitlore easier to install by documenting Homebrew as an installation method in the README. The install section now offers Homebrew as a first-class option alongside the existing Go install path, with the correct tap URL pointing to the developer's Homebrew tap repository. No work was left incomplete, though the Homebrew formula itself would need to exist in the tap for this to function end-to-end.
